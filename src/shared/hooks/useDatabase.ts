import useDatabase from '@nozbe/watermelondb/react/useDatabase';

/**
 * Re-exports WatermelonDB's useDatabase for a consistent import path.
 * Import from '@shared/hooks/useDatabase' instead of WatermelonDB directly.
 */
export { useDatabase };
